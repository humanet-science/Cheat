# General game settings
game:
  seed: 42
  experimental_mode: False
  out_dir: ~/Cheat_game_data/
  note: ~ # Additional note to add to the output directory

# Player settings (non-human)
players:
  - type: LLM
    name: DeepSeek
    avatar: 🦊
    kind: deepseek
    model_kwargs:
      model: deepseek-chat
      stream: False
  - type: SmartBot
    name: B0Ttled Up
    avatar: 🍾
    verbosity: 0.1
  - type: SmartBot
    name: Beep Boop
    avatar: 🤖
    verbosity: 0.1
  - type: SmartBot
    name: ChattyBot
    avatar: 🐭
    verbosity: 0.3
  - type: SmartBot
    name: B0Tanical
    avatar: 🌼
    verbosity: 0.2

# Settings specifically for the experimental mode
experiment:
  predefined_messages:
    - "No way!"
    - "That's some BS"
    - "Flip that"
    - "Classic"
    - "Liars be lyin"
    - "Damn"
    - "Gosh"
    - "Nice"

# Default system prompt for LLMs; this will be formatted
default_system_prompt: > 
  You are playing Cheat with {N_players} other players. You are Player {player_id}. 
  Goal: get rid of all cards. On your turn: play 1-3 cards face down and declare a rank (Aces cannot be declared). 
  You can bluff - cards don't need to match declaration. 
  Options: 'Call' (challenge previous play), 'Play [cards]' (follow current rank), or 'Play [cards]; Declare rank' 
  (start new rank when pile empty). Four-of-a-kind auto-discard. 
  Win by emptying hand. 
  FORMAT REQUIREMENTS:
  - Respond ONLY with: 'Call', 'Play [card1, card2]', or 'Play [cards]; Declare rank' 
  - Use J/Q/K/A for face cards
  - Examples: 'Play [4♣, 4♦]; Declare 4', 'Play [A♠, K♥]', 'Play [2♥, J♣]; Declare J' 
  - NO extra text: backend parsing depends on exact format.
                                                                                                                                                      - Respond ONLY with: 'Call', 'Play [card1, card2]', or 'Play [cards]; Declare rank' - Use J/Q/K/A for face cards - Examples: 'Play [4♣, 4♦]; Declare 4', 'Play [A♠, K♥]', 'Play [2♥, J♣]; Declare J' - NO extra text: backend parsing depends on exact format."